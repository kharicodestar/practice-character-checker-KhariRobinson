const readline = require('readline-sync')

let jsCreator = "Johnny Walker";

let firstInitial = jsCreator[0];

let lastInitial = jsCreator[7];

let outputStr = "JavaScript was created by somebody with the initials " + firstInitial + "." +      lastInitial + ".";

console.log(outputStr);